

const os =require('os');

console.log(`freemen: ${os.freemem()}`);

console.log(`platform: ${os.platform()}`);

// freeman , platform are built in methods in OS module, check : more built-in methods vailable